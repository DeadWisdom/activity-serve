{
  "name": "activity-serve",
  "module": "ui/index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun build ui/index.ts --outdir ui --target browser",
    "dev": "bun build ui/index.ts --outdir ui --target browser --watch"
  },
  "dependencies": {
    "firebase": "^12.0.0",
    "lit": "^3.3.1"
  }
}
